# 교수님 확인용 구현 현황

## 1. 프로젝트 요약

- 프로젝트명: Evidence-based Career Agent
- 목적: 대학생의 수업, 성적, 프로젝트, 자격증, 활동 기록을 바탕으로 목표 진로 기준 부족 역량과 4주 실행 로드맵을 안내한다.
- 실행 명령: `python career_agent.py`
- 입력 파일: `sample_profile.json`
- 출력 파일: `output.md`, `output_user_guide.md`, `review_report.md`
- 구현 수준: 기본형
- 외부 API 사용 여부: 사용하지 않음
- LLM 사용 여부: 사용하지 않음
- 구현 방식: Python 기본 라이브러리 기반 Rule-based 분석

## 2. Week10 구현 내용

### 구현 완료

- 프로젝트 목표와 MVP 범위를 문서로 정리했다.
- 작업 계획과 TODO를 별도 문서로 관리했다.
- 코딩에이전트 사용 기록을 `tool_usage_log.md`에 남겼다.
- Copilot 작업 규칙 파일을 커리어 에이전트 실행 기준으로 정리했다.

### 실제 수행 기능

- `context-career-agent.md`: 문제 정의, 입력/출력, MVP 범위 정리
- `plan-career-agent.md`: Rule-based only 구현 계획 정리
- `todo-career-agent.md`: 구현 단계와 완료 상태 정리
- `.github/copilot-instructions.md`: 기본 실행 명령과 작업 규칙 정리
- `tool_usage_log.md`: 실제 사용한 Codex와 미사용 도구 기록

### 구현 예정 또는 부족한 부분

- GitHub 저장소 URL은 아직 입력하지 않았다.

## 3. Week11 구현 내용

### 구현 완료

- Python 기본 코드로 실행되는 커리어 에이전트를 구현했다.
- `sample_profile.json`을 입력으로 읽는다.
- 터미널에 Markdown 형식 분석 결과를 출력한다.

### 실제 수행 기능

- `load_profile()`: JSON 입력 파일 읽기
- `analyze_profile_strengths()`: 수업, 성적, 프로젝트, 자격증, 활동 기반 강점 분석
- `main()`: 전체 파이프라인 실행

### 구현 예정 또는 부족한 부분

- 수업 예시 파일명은 `my_agent.py`, `sample_input.txt`이지만 현재 프로젝트는 `career_agent.py`, `sample_profile.json`을 사용한다.

## 4. Week12 구현 내용

### 구현 완료

- 입력 분석, 판단 기준, 중간 정보, 결과 저장 흐름을 구성했다.
- 목표 진로 하나를 기준으로 필요 역량과 현재 증거를 비교한다.
- 결과를 파일로 저장한다.

### 실제 수행 기능

- `get_required_skills()`: 목표 진로별 필요 역량 반환
- `analyze_gaps()`: 현재 증거와 필요 역량 비교
- `recommend_activities()`: 부족 역량별 추천 활동 생성
- `save_outputs()`: 제출용 결과 파일 3개 저장

### 생성 결과

- `output.md`: 현재 강점, 부족 역량, 보유 근거
- `output_user_guide.md`: 추천 활동, 4주 실행 로드맵
- `review_report.md`: 실행 결과 점검

### 구현 예정 또는 부족한 부분

- 판단 기준은 키워드 기반이므로 입력 표현이 바뀌면 결과가 달라질 수 있다.
- 목표 진로별 필요 역량 목록은 코드에 정의된 범위로 제한된다.

## 5. Week13 구현 내용

### 구현 완료

- 최종 사용자 출력문을 생성한다.
- 검토자 결과 파일을 생성한다.
- 부족 역량을 추천 활동과 4주 로드맵으로 연결한다.

### 실제 수행 기능

- `write_user_guide_markdown()`: 사용자 실행 가이드 작성
- `make_roadmap()`: Week 1~4 실행 계획 생성
- `write_review_report()`: 입력, 목표 진로, 부족 역량, 추천 활동, 로드맵 생성 여부 점검

### 구현 예정 또는 부족한 부분

- 검토자는 규칙 기반 점검 수준이다.
- 자연어 품질을 깊게 평가하는 LLM 검토자는 구현하지 않았다.

## 6. Week14 구현 내용

### 구현 완료

- README를 작성했다.
- 출력 예시 파일 3개를 생성했다.
- 도구 사용 기록을 작성했다.
- 발표자료 초안을 작성했다.
- 최종 점검 문서를 작성했다.

### 실제 수행 기능

- `README.md`: 프로젝트 설명, 실행 방법, 입력/출력 파일, 에이전트 역할, 구현 수준, 한계 정리
- `tool_usage_log.md`: Codex 사용 기록과 외부 API 미사용 기록
- `presentation.md`: 발표 초안과 1분 데모 흐름 정리
- `week14_final_check.md`: 최종 점검 항목 정리

### 구현 예정 또는 부족한 부분

- GitHub 저장소 URL은 제출 직전에 입력해야 한다.

## 7. 최종 제출 파일 점검표

| 파일명 | 존재 여부 | 역할 | 비고 |
|---|---|---|---|
| `career_agent.py` | 있음 | 최종 실행 코드 | `python career_agent.py`로 실행 |
| `sample_profile.json` | 있음 | 입력 예시 파일 | 학생 프로필 예시 |
| `output.md` | 있음 | 핵심 분석 결과 | 현재 강점, 부족 역량, 보유 근거 |
| `output_user_guide.md` | 있음 | 사용자 실행 가이드 | 추천 활동, 4주 로드맵 |
| `review_report.md` | 있음 | 검토자 결과 | 점검 결과와 한계 |
| `README.md` | 있음 | 프로젝트 설명 | 실행 방법과 구현 수준 포함 |
| `context-career-agent.md` | 있음 | 프로젝트 맥락 | 목표, 입력/출력, MVP 범위 |
| `todo-career-agent.md` | 있음 | 개발 TODO | GitHub URL 입력만 미완료 |
| `plan-career-agent.md` | 있음 | 구현 계획 | Rule-based only 기준 |
| `.github/copilot-instructions.md` | 있음 | Copilot 작업 규칙 | 커리어 에이전트 기준으로 정리 |
| `tool_usage_log.md` | 있음 | 도구 사용 기록 | Codex 사용, 나머지 도구 미사용 |
| `presentation.md` | 있음 | 발표자료 초안 | 1분 데모 흐름 포함 |
| `week14_final_check.md` | 있음 | 최종 점검 메모 | GitHub URL 입력 필요 |
| `my_agent.py` | 없음 | 수업 예시 실행 파일명 | 현재는 `career_agent.py` 사용 |
| `sample_input.txt` | 없음 | 수업 예시 입력 파일명 | 현재는 `sample_profile.json` 사용 |

## 8. 최종 구현 수준 및 한계

### 최종 구현 수준

- 기본형: 규칙 기반 함수 에이전트
- 외부 API 없이 실행 가능
- 실행 명령 하나로 터미널 출력과 제출용 결과 파일 생성 가능

### 구현 완료

- 입력 읽기
- 현재 강점 분석
- 목표 진로 기준 필요 역량 정의
- 부족 역량 분석
- 추천 활동 생성
- 4주 실행 로드맵 생성
- 결과 파일 저장
- 검토 보고서 생성
- README, 발표자료, 도구 사용 기록 작성

### 구현하지 않은 것

- LLM API 연동
- Groq API 연동
- Docker
- LangGraph
- RAG
- VectorDB
- 크롤링
- Calendar API
- 채용공고 자동 분석

### 현재 한계

- 키워드 기반 분석이라 입력 표현이 크게 바뀌면 부정확할 수 있다.
- 목표 진로별 필요 역량은 코드에 정의된 규칙에 의존한다.
- 관심 분야는 참고 정보이며 실제 보유 역량으로 바로 인정하지 않는다.
- GitHub 저장소 URL은 아직 문서에 입력되지 않았다.

## 9. 교수님께 강조할 구현 포인트

- 새 기능 확장보다 Week14 제출 기준에 맞춘 실행 가능 상태를 우선했다.
- `python career_agent.py` 한 번으로 분석 결과와 제출용 출력 파일 3개가 생성된다.
- 에이전트 역할을 함수 단위로 나누었다.
- 외부 API나 LLM 없이 기본형 규칙 기반으로 안정적으로 실행된다.
- 결과는 단순 추천이 아니라 현재 강점, 부족 역량, 추천 활동, 4주 실행 계획으로 이어진다.
- 구현하지 않은 확장 기능과 현재 한계를 문서에 명확히 적었다.
